name: Actualizar Resultados Lotto Activo

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  actualizar:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          
      - name: Obtener HTML con User-Agent de navegador REAL
        run: |
          echo "🔍 Obteniendo HTML de loteriadehoy.com con User-Agent de Chrome..."
          curl -s -L \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" \
            -H "Accept: text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,*/*;q=0.8" \
            -H "Accept-Language: es-ES,es;q=0.9,en;q=0.8" \
            -H "Referer: https://www.google.com/" \
            -H "DNT: 1" \
            -H "Connection: keep-alive" \
            -H "Upgrade-Insecure-Requests: 1" \
            "https://loteriadehoy.com/animalito/lottoactivo/resultados/" > pagina_completa.html
          echo "✅ HTML guardado. Tamaño: $(wc -l < pagina_completa.html) líneas"
          echo "📄 Primeras 3 líneas del HTML:"
          head -3 pagina_completa.html
          
      - name: MOSTRAR el HTML (para depuración)
        run: |
          echo "📄 PRIMERAS 50 LÍNEAS DEL HTML:"
          echo "========================================"
          head -50 pagina_completa.html
          echo "========================================"
          echo ""
          echo "🔍 BUSCANDO PATRONES DE NÚMEROS:"
          echo "========================================"
          grep -E '<h4>[0-9]+' pagina_completa.html | head -10
          echo "========================================"
          
      - name: Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Extraer números con EXPRESIÓN CORRECTA
        run: |
          cat > extraer.js << 'EOF'
          const fs = require('fs');
          
          console.log("🔍 Leyendo HTML guardado...");
          const html = fs.readFileSync('pagina_completa.html', 'utf8');
          
          console.log("📊 Buscando números en el HTML...");
          
          // MÉTODO 1: Buscar patrón exacto de loteriadehoy
          const regex = /<h4>(\d{1,2})\s+([A-Za-zÁ-ú]+)<\/h4>/g;
          const numeros = [];
          let match;
          
          console.log("\n📌 Números encontrados en <h4>:");
          while ((match = regex.exec(html)) !== null) {
            const numero = parseInt(match[1]);
            const animal = match[2];
            console.log(`  ✓ ${numero} - ${animal}`);
            numeros.push(numero);
          }
          
          console.log(`\n📊 Total encontrados: ${numeros.length}`);
          
          // Guardar resultados
          const datos = {
            resultados: numeros,
            fecha_actualizacion: new Date().toISOString(),
            total_resultados: numeros.length,
            html_preview: html.substring(0, 200)
          };
          
          fs.writeFileSync('resultados.json', JSON.stringify(datos, null, 2));
          console.log('✅ resultados.json guardado');
          EOF
          
      - name: Ejecutar extracción
        run: node extraer.js
        
      - name: Verificar resultado
        run: cat resultados.json
        
      - name: Guardar HTML para inspección (opcional)
        run: |
          git add pagina_completa.html || true
          
      - name: Guardar cambios
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin main
          git reset --soft origin/main
          git add resultados.json
          if git diff --staged --quiet; then
            echo "ℹ️ Sin cambios en resultados.json"
          else
            git commit -m "🔄 Actualización automática $(date +'%Y-%m-%d %H:%M')"
            git push origin HEAD:main
            echo "✅ Cambios subidos"
          fi
